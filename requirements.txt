# OddsEngine — Streamlit Cloud requirements
# Verified compatible with Streamlit Cloud's Python 3.11 default.

# ---- Frontend ----
streamlit>=1.32,<2.0

# ---- Data ----
pandas>=2.0,<3.0
requests>=2.31,<3.0
plotly>=5.20,<6.0

# ---- Database (Supabase Postgres via REST) ----
supabase>=2.0,<3.0

# ---- Apify (PrizePicks + Underdog ingest) ----
apify-client>=1.7,<2.0

# ---- LLM providers (pick one or both) ----
anthropic>=0.34,<1.0
google-generativeai>=0.7,<1.0
